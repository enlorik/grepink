# Required. Set on Railway via the PostgreSQL plugin reference variable.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Required. Generate with: openssl rand -hex 32
SYNC_TOKEN=replace-with-a-strong-random-token

# Optional. Railway sets this automatically; defaults to 3000 locally.
PORT=3000
